### 浏览器登陆状态检测（需求说明）

#### 1. 背景与目标
- **目标**: 当批量下载出现“持续失败且无任何下载速度”的异常时，自动检测当前 SharePoint 登录状态，并在需要时引导用户完成登录，恢复下载能力。

#### 2. 触发条件（WHEN）
- **满足以下任一条件触发登录状态检测**：
  - 连续 N 次（默认 2 次，可配置）下载失败且失败原因非网络超时之外的可重试错误。
  - 当前无任何处于 DOWNLOADING 状态的任务，且全局下载速度为 0 持续 T 秒（默认 15s，可配置）。

#### 3. 登录状态检测流程（FLOW）
1) **检测是否存在已打开的浏览器实例**
   - 首选方式：尝试连接既有调试端口（默认 `9222`）的浏览器（CDP 连接）。
   - 备用方式：进程扫描（chrome/msedge/chromium）仅用于提示，不做强依赖。

2) **若存在已打开的浏览器**
   - 在该浏览器内打开目标地址 `SP_TARGET_URL`（来自 `.env` 或 `settings.target_url`）。
   - 依据页面特征判断：
     - 若检测到登录表单/登录重定向（如存在用户名/密码输入框、Microsoft 登录页特征），则判定“未登录”。
     - 若能正常加载 SharePoint 文档列表页，判定“已登录”。

3) **若判定未登录或没有已打开浏览器**
   - 调用并提示运行脚本：`python scripts/start_debug_browser.py`
   - 引导用户在新启动的浏览器中完成手动登录。脚本会以 `--remote-debugging-port=9222` 启动浏览器。
   - 用户完成登录并按回车后，恢复下载流程（重新扫描/继续任务）。

#### 4. 与现有系统的集成点（INTEGRATION）
- 与批量下载主流程（如 `scripts/scan_with_existing_browser_cdp.py`）集成：
  - 在下载重试与全局速度监控逻辑中加入触发器。
  - 在触发后执行“登录状态检测流程”。
- 统一读取目标地址：
  - 优先使用 `.env` 中 `SP_TARGET_URL`，并在 `src/sp_automation/config.py` 的 `settings.target_url` 暴露。
  - 各脚本避免硬编码 URL，统一改为读取 `settings.target_url`。

#### 5. 配置项（CONFIG）
- `SP_TARGET_URL`: 目标地址（必填，.env）。
- `LOGIN_CHECK_MAX_FAILS`（可选，默认 2）: 连续失败阈值。
- `LOGIN_CHECK_IDLE_SECONDS`（可选，默认 15）: 全局下载速度为 0 的持续秒数阈值。
- `DEBUG_PORT`（可选，默认 9222）: 浏览器调试端口。

#### 6. 日志与可观测性（LOGGING）
- 在控制台与统一 JSON 日志中新增登录检测相关节点：
  - `login_check.triggered`: true/false
  - `login_check.reason`: failed_consecutively / zero_speed / manual
  - `login_check.browser_found`: true/false（是否连上 CDP）
  - `login_check.page_status`: logged_in / login_required / unknown
  - `login_check.action`: open_target_url / start_debug_browser / none
  - `login_check.completed`: true/false

#### 7. 伪代码（PSEUDOCODE）
```
if consecutive_failures >= LOGIN_CHECK_MAX_FAILS or global_speed_zero_for(LOGIN_CHECK_IDLE_SECONDS):
    log(login_check.triggered = true)
    if connect_cdp(port=DEBUG_PORT):
        open(settings.target_url)
        if is_login_page():
            log(page_status=login_required, action=start_debug_browser)
            prompt_run("python scripts/start_debug_browser.py")
            wait_user_confirm()
        else:
            log(page_status=logged_in, action=none)
    else:
        log(browser_found=false, action=start_debug_browser)
        prompt_run("python scripts/start_debug_browser.py")
        wait_user_confirm()
    log(login_check.completed = true)
    resume_batch_download()
```

#### 8. 验收标准（ACCEPTANCE CRITERIA）
- 当满足触发条件时，系统会：
  - 正确检测并记录是否连上已开启调试端口的浏览器。
  - 能在已开启浏览器中打开 `SP_TARGET_URL` 并判断登录状态。
  - 若未登录或无浏览器，提示启动 `start_debug_browser.py` 并在登录后可继续下载。
- 日志文件与控制台输出包含登录检测的关键步骤与结果。
- 不破坏现有“流水线式并行下载 + 后台解压 + 实时日志”的行为与安全约束。

#### 9. 安全与合规（SECURITY）
- 严格遵守《SharePoint 安全操作指南》：只读、路径限制、操作白名单、URL 校验。
- 不在代码中硬编码敏感信息，全部通过 `.env` 管理。
- 登录过程为人工完成，脚本仅负责打开浏览器与目标地址。

#### 10. 回退策略（ROLLBACK）
- 若登录检测失败或用户取消登录，下载流程进入“暂停待人工介入”状态，并在日志中标记。


