# SharePoint 文件下载功能使用说明

## 功能概述

基于您的发现，我们实现了基于右键菜单的SharePoint文件下载功能。该功能模块化设计，位于 `src/sp_automation/file_downloader.py` 中。

## 核心特性

1. **右键下载**: 直接在文件上右键点击，从菜单栏中选择下载选项
2. **安全下载**: 集成安全管理器，确保下载路径和操作的安全性
3. **批量下载**: 支持批量下载多个文件
4. **交互式下载**: 用户可以选择逐个确认或批量下载
5. **测试模式**: 支持测试模式，需要用户确认每个下载操作

## 使用方法

### 1. 在扫描脚本中使用

扫描完成后，脚本会自动检测到可下载的文件并询问是否开始下载：

```bash
python scripts/scan_with_existing_browser.py
```

### 2. 直接使用下载模块

```python
from src.sp_automation.file_downloader import SharePointFileDownloader
from src.sp_automation.safe_operations import SharePointSafetyManager

# 创建安全管理器
safety_manager = SharePointSafetyManager(
    allowed_download_path="downloads",
    allowed_sharepoint_path="Test",
    test_mode=True
)

# 创建下载器
downloader = SharePointFileDownloader(safety_manager)

# 下载文件
await downloader.download_file_by_right_click(page, file_element, filename)
```

## 文件结构

```
src/sp_automation/
├── file_downloader.py          # 新的下载模块
├── safe_operations.py          # 安全管理器（已存在）
└── ...

scripts/
├── scan_with_existing_browser.py  # 主扫描脚本（已更新）
└── test_download.py               # 下载功能测试脚本
```

## 下载流程

1. **文件检测**: 扫描过程中自动收集可下载的文件信息
2. **用户选择**: 扫描完成后询问是否开始下载
3. **下载方式选择**: 
   - 下载所有文件
   - 逐个确认下载
   - 跳过下载
4. **右键下载**: 对每个文件执行右键点击 → 选择下载选项
5. **安全保存**: 文件保存到指定的安全目录

## 安全特性

- **路径限制**: 只能下载到指定的安全目录
- **URL验证**: 确保只在允许的SharePoint路径下操作
- **操作白名单**: 只允许安全的下载操作
- **用户确认**: 测试模式下需要用户确认每个下载操作

## 支持的下载选项

下载器会尝试查找以下下载选项文本：
- "下载"
- "Download" 
- "下载副本"
- "Download a copy"

## 注意事项

1. 需要浏览器以调试模式运行（端口9222）
2. 确保已登录SharePoint并有相应权限
3. 下载路径必须在允许的目录内
4. 测试模式下每个下载都需要用户确认

## 故障排除

### 常见问题

1. **未找到下载选项**: 检查右键菜单是否出现，可能需要调整等待时间
2. **下载失败**: 检查网络连接和文件权限
3. **路径错误**: 确认下载路径在允许的目录内

### 调试建议

1. 启用详细日志输出
2. 检查浏览器控制台错误
3. 验证文件元素定位是否正确
4. 确认SharePoint页面加载完成

## 扩展功能

该模块设计为可扩展的，可以轻松添加：
- 更多下载选项检测
- 下载进度显示
- 断点续传功能
- 并发下载支持
