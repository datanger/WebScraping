# WebScraping 项目架构设计指南

## 🏗️ 架构概述

本项目采用**分层架构设计**，将**通用功能模块**和**业务逻辑**完全分离，让技术人员能够专注于业务流程的开发和测试。

### 核心设计原则

1. **模块化**：通用功能模块化，业务逻辑独立
2. **可复用**：通用模块可在不同业务场景中复用
3. **易维护**：业务逻辑与通用功能分离，便于维护
4. **可扩展**：新业务需求只需开发新的业务脚本

---

## 📁 项目结构

```
WebScraping/
├── src/sp_automation/          # 通用功能模块（无业务逻辑）
│   ├── __init__.py            # 模块入口
│   ├── browser.py             # 浏览器管理
│   ├── config.py              # 配置管理
│   ├── session.py             # 会话管理
│   ├── extractor.py           # 数据抽取
│   ├── element_detector.py    # 元素检测
│   ├── page_operations.py     # 页面操作
│   ├── safe_operations.py     # 安全操作管理
│   └── otp_providers.py       # OTP认证
├── scripts/                   # 业务逻辑脚本
│   ├── business_template.py   # 业务脚本模板
│   ├── sharepoint_business.py # SharePoint业务示例
│   ├── safe_sharepoint_downloader.py # 安全SharePoint下载器
│   ├── run_scrape.py          # 简单抓取脚本
│   ├── extract_page.py        # 页面抽取脚本
│   ├── init_session.py        # 会话初始化脚本
│   └── advanced_scraper.py    # 高级抓取命令行工具
├── examples/                  # 示例文件
│   └── article_schema.json    # Schema示例
├── 架构设计指南.md           # 架构设计文档
├── 需求文档.md               # 项目需求文档
└── requirements.txt           # 依赖配置
```

---

## 🔧 通用模块说明 (src/sp_automation/)

### 1. **browser.py** - 浏览器管理
**职责**：管理浏览器实例和上下文创建
**功能**：
- 支持多种浏览器类型（Chromium/Edge/Firefox）
- 支持持久化上下文模式
- 反检测配置
- 代理和网络设置

**使用示例**：
```python
from sp_automation.browser import create_context, close_all

# 创建浏览器上下文
pw, browser, context = await create_context(
    browser_type="edge",
    persistent=True,
    user_data_dir="storage/profile"
)
```

### 2. **config.py** - 配置管理
**职责**：集中管理所有配置项
**功能**：
- 环境变量支持
- 默认值配置
- 类型转换
- 配置验证

**使用示例**：
```python
from sp_automation.config import settings

print(settings.browser_type)  # 获取浏览器类型
print(settings.target_url)   # 获取目标URL
```

### 3. **session.py** - 会话管理
**职责**：管理浏览器会话的保存和恢复
**功能**：
- 会话状态保存
- Cookies恢复
- 持久化会话初始化
- 会话有效性检测

**使用示例**：
```python
from sp_automation.session import init_persistent_session, save_session_state

# 初始化持久化会话
pw, browser, context = await init_persistent_session()

# 保存会话状态
await save_session_state(context)
```

### 4. **page_operations.py** - 页面操作
**职责**：提供通用的页面操作方法
**功能**：
- 页面导航
- 元素点击
- 文本提取
- 悬停操作
- 截图功能

**使用示例**：
```python
from sp_automation.page_operations import navigate_to_page, click_element_by_text

# 导航到页面
page = await navigate_to_page(context, "https://example.com")

# 点击元素
await click_element_by_text(page, "登录")
```

### 5. **element_detector.py** - 元素检测
**职责**：检测和分析页面元素
**功能**：
- 按钮检测
- 元素信息提取
- 悬停交互
- 动态元素检测

**使用示例**：
```python
from sp_automation.element_detector import detect_buttons_on_page

# 检测页面按钮
result = await detect_buttons_on_page(page)
print(f"检测到 {len(result.buttons)} 个按钮")
```

### 6. **safe_operations.py** - 安全操作管理
**职责**：提供安全的SharePoint文件操作
**功能**：
- SharePoint路径解析
- 安全操作验证
- 测试模式支持
- 用户确认机制
- 路径限制检查

**使用示例**：
```python
from sp_automation.safe_operations import SharePointSafetyManager, safe_click_element_with_confirmation

# 创建安全管理器
safety_manager = SharePointSafetyManager(
    allowed_download_path="downloads",
    allowed_sharepoint_path="Test",
    test_mode=True
)

# 安全点击元素（带确认）
await safe_click_element_with_confirmation(
    page, safety_manager, "#download-btn", "下载文件"
)
```

### 7. **extractor.py** - 数据抽取
**职责**：基于Schema的数据抽取
**功能**：
- Schema驱动抽取
- 多种选择器支持
- 列表数据抽取
- 错误处理

**使用示例**：
```python
from sp_automation.extractor import load_schema, extract_by_schema

# 加载Schema
schema = load_schema("schema.json")

# 执行抽取
result = await extract_by_schema(page, schema)
```

### 7. **otp_providers.py** - OTP认证
**职责**：多因素认证支持
**功能**：
- TOTP支持
- IMAP邮箱验证码
- 人工输入支持

**使用示例**：
```python
from sp_automation.otp_providers import get_otp_from_provider

# 获取OTP
otp = get_otp_from_provider()
```

---

## 📝 业务脚本开发指南 (scripts/)

### 开发原则

1. **基于通用模块**：所有业务逻辑都基于 `src/` 模块实现
2. **专注业务逻辑**：只关注业务流程，不重复实现通用功能
3. **模块化设计**：将复杂业务拆分为多个步骤
4. **错误处理**：完善的异常处理和结果反馈

### 业务脚本模板

使用 `business_template.py` 作为开发起点：

```python
class YourBusinessLogic:
    def __init__(self, browser_type: str = "chromium"):
        self.browser_type = browser_type
    
    async def __aenter__(self):
        # 初始化浏览器上下文
        self.playwright, self.browser, self.context = await init_persistent_session(
            headless=False,
            browser_type=self.browser_type
        )
        return self
    
    async def __aexit__(self, exc_type, exc_val, exc_tb):
        # 清理资源
        if self.playwright and self.browser:
            await close_all(self.playwright, self.browser)
    
    async def step_1_your_business_logic(self):
        """业务步骤1：具体业务逻辑"""
        # 使用通用模块实现业务需求
        pass
    
    async def step_2_another_business_logic(self):
        """业务步骤2：另一个业务逻辑"""
        # 使用通用模块实现业务需求
        pass
```

### 开发步骤

1. **复制模板**：复制 `business_template.py` 为新的业务脚本
2. **定义业务类**：创建专门的业务逻辑类
3. **实现业务步骤**：将业务流程拆分为多个步骤
4. **安全考虑**：对于SharePoint操作，使用安全操作模块
5. **使用通用模块**：调用 `src/` 中的通用功能
6. **测试和调试**：使用可视化模式测试业务逻辑
7. **保存结果**：将业务结果保存为JSON格式

### 示例：SharePoint 业务脚本

参考 `sharepoint_business.py` 的实现：

```python
class SharePointBusinessLogic:
    async def login_to_sharepoint(self, login_url: str):
        """业务逻辑：登录到 SharePoint"""
        page = await navigate_to_page(self.context, login_url)
        # ... 具体登录逻辑
    
    async def navigate_to_document_library(self, library_url: str):
        """业务逻辑：导航到文档库"""
        page = await navigate_to_page(self.context, library_url)
        # ... 具体导航逻辑
    
    async def detect_document_actions(self, library_url: str):
        """业务逻辑：检测文档操作按钮"""
        button_result = await detect_buttons_on_page(page)
        # ... 具体检测逻辑
```

---

## 🚀 使用指南

### 1. 环境配置

```bash
# 安装依赖
pip install -r requirements.txt

# 安装浏览器
playwright install chromium
playwright install msedge

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件
```

### 2. 开发新业务脚本

```bash
# 复制模板
cp scripts/business_template.py scripts/my_business.py

# 编辑业务逻辑
vim scripts/my_business.py

# 运行测试
python scripts/my_business.py
```

### 3. 使用现有脚本

```bash
# 初始化会话
python scripts/init_session.py

# 执行简单抓取
python scripts/run_scrape.py

# 使用高级命令行工具
python scripts/advanced_scraper.py --mode detect --url "https://example.com"

# 使用业务脚本模板
python scripts/business_template.py

# 运行 SharePoint 业务示例
python scripts/sharepoint_business.py

# 使用安全 SharePoint 下载器
python scripts/safe_sharepoint_downloader.py \
  --url "https://sharepoint.com/..." \
  --test-mode \
  --allowed-path "Test"
```

---

## 🔄 开发工作流

### 1. **需求分析**
- 明确业务需求
- 识别需要的通用功能
- 设计业务流程

### 2. **选择通用模块**
- 从 `src/sp_automation/` 中选择需要的模块
- 了解模块的API和功能
- 设计模块组合方案

### 3. **开发业务脚本**
- 基于模板创建业务脚本
- 实现具体的业务逻辑
- 使用通用模块完成功能

### 4. **测试和调试**
- 使用可视化模式测试
- 使用测试模式验证操作
- 逐步验证业务步骤
- 处理异常情况

### 5. **部署和运行**
- 配置生产环境
- 设置自动化运行
- 监控和日志记录

---

## 📊 最佳实践

### 1. **模块使用**
- 优先使用 `src/` 中的通用模块
- 避免重复实现通用功能
- 保持业务逻辑的简洁性

### 2. **错误处理**
- 每个业务步骤都要有错误处理
- 提供详细的错误信息
- 支持重试机制

### 3. **配置管理**
- 使用环境变量管理配置
- 提供合理的默认值
- 支持不同环境的配置

### 4. **日志记录**
- 记录关键业务步骤
- 提供进度反馈
- 保存执行结果

### 5. **资源管理**
- 使用异步上下文管理器
- 及时释放浏览器资源
- 处理异常情况下的资源清理

---

## 🎯 总结

这个架构设计的核心优势：

1. **开发效率高**：技术人员只需关注业务逻辑，通用功能已封装好
2. **维护成本低**：通用模块和业务逻辑分离，便于维护和升级
3. **复用性强**：通用模块可在不同业务场景中复用
4. **扩展性好**：新业务需求只需开发新的业务脚本
5. **测试友好**：业务逻辑独立，便于单元测试和集成测试

通过这种架构设计，技术人员可以：
- 快速开发新的业务脚本
- 专注于业务流程的实现
- 复用成熟的通用功能
- 提高开发效率和代码质量

---

*文档版本: 1.0*  
*最后更新: 2024年*  
*维护者: 项目开发团队*
