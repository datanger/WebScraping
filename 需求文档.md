# WebScraping 项目需求文档

## 1. 项目概述

### 1.1 项目名称
WebScraping - 基于 Playwright 的自动化网页抓取工具

### 1.2 项目目标
开发一个功能完整的网页抓取自动化工具，特别针对 SharePoint 等需要身份验证的网站，支持会话持久化、多因素认证（MFA）和灵活的数据抽取规则。

### 1.3 技术栈
- **核心框架**: Playwright (Python)
- **配置管理**: python-dotenv
- **重试机制**: tenacity
- **OTP 支持**: pyotp
- **开发语言**: Python 3.8+

## 2. 功能需求

### 2.1 浏览器自动化模块 (browser.py)

#### 2.1.1 核心功能
- **浏览器上下文创建**: 支持创建可配置的浏览器上下文
- **会话持久化**: 支持加载和保存浏览器会话状态
- **代理支持**: 可配置 HTTP/HTTPS 代理
- **性能控制**: 支持 headless 模式和慢动作模式

#### 2.1.2 技术规格
- 基于 Playwright Chromium 引擎
- 支持存储状态文件加载（用于会话复用）
- 可配置超时时间（默认 30 秒）
- 支持中文本地化设置
- 忽略 HTTPS 证书错误
- 禁用 CSP（内容安全策略）

### 2.2 数据抽取模块 (extractor.py)

#### 2.2.1 抽取规则定义
- **字段规则 (FieldRule)**:
  - 支持 CSS 和 XPath 选择器
  - 支持文本、属性、HTML 内容提取
  - 可配置是否只取第一个匹配元素
  - 支持必填字段验证

- **列表规则 (ListRule)**:
  - 支持列表项容器选择器
  - 可对列表项应用子字段规则
  - 支持批量数据提取

#### 2.2.2 抽取功能
- **通用字段抽取**: 基于选择器提取单个或多个字段
- **列表数据抽取**: 批量提取列表结构数据
- **Schema 驱动**: 基于 JSON Schema 定义抽取规则
- **错误处理**: 优雅处理选择器匹配失败情况

### 2.3 会话管理模块 (session.py)

#### 2.3.1 会话初始化
- **自动登录**: 支持自动处理登录流程
- **MFA 支持**: 集成多种多因素认证方式
- **会话保存**: 自动保存登录状态到文件
- **人工干预**: 支持人工完成复杂登录流程

#### 2.3.2 会话复用
- **状态恢复**: 从文件恢复浏览器会话状态
- **无人值守**: 支持基于已保存会话的自动化运行
- **会话验证**: 自动检测会话有效性

### 2.4 OTP 认证模块 (otp_providers.py)

#### 2.4.1 TOTP 支持
- **标准兼容**: 支持 RFC 6238 TOTP 标准
- **密钥格式**: 支持 Base32 和原始密钥格式
- **时间同步**: 自动处理时间窗口和计数器

#### 2.4.2 IMAP 邮箱验证码
- **邮箱轮询**: 自动轮询 IMAP 邮箱获取验证码
- **邮件解析**: 智能解析邮件主题和正文
- **验证码提取**: 自动提取 6 位数字验证码
- **超时控制**: 可配置轮询超时时间

### 2.5 SharePoint 集成模块 (sharepoint.py)

#### 2.5.1 导航功能
- **页面导航**: 自动导航到 SharePoint 文档库
- **加载等待**: 智能等待页面完全加载
- **网络空闲检测**: 等待网络请求完成

#### 2.5.2 元素操作
- **文本提取**: 通用文本内容提取
- **按钮点击**: 基于文本的按钮点击操作
- **元素等待**: 智能等待元素可见

### 2.6 配置管理模块 (config.py)

#### 2.6.1 环境配置
- **环境变量**: 支持 .env 文件配置
- **默认值**: 提供合理的默认配置
- **类型转换**: 自动类型转换和验证

#### 2.6.2 配置项
- **登录凭据**: 邮箱、密码配置
- **目标 URL**: SharePoint 站点地址
- **浏览器设置**: headless、代理、超时等
- **MFA 配置**: OTP 提供器、IMAP 设置等

### 2.7 抓取执行模块 (scraping.py)

#### 2.7.1 抓取功能
- **页面抓取**: 基于持久化会话的页面抓取
- **数据提取**: 集成抽取模块进行数据提取
- **结果格式化**: 结构化输出抓取结果

## 3. 命令行工具

### 3.1 会话初始化脚本 (init_session.py)
- **功能**: 初始化并保存浏览器会话
- **使用场景**: 首次登录或会话过期时使用
- **交互模式**: 支持人工完成复杂登录流程

### 3.2 抓取执行脚本 (run_scrape.py)
- **功能**: 基于已保存会话执行抓取任务
- **无人值守**: 支持自动化批量抓取
- **结果输出**: 结构化输出抓取结果

### 3.3 页面抽取脚本 (extract_page.py)
- **功能**: 基于 Schema 规则抽取指定页面数据
- **参数化**: 支持命令行参数配置
- **输出格式**: JSON 格式输出抽取结果

## 4. 使用场景

### 4.1 SharePoint 文档库抓取
- **目标**: 自动抓取 SharePoint 文档库内容
- **特点**: 需要身份验证和会话管理
- **应用**: 企业内容管理、文档归档

### 4.2 通用网页数据抽取
- **目标**: 基于 Schema 规则抽取任意网页数据
- **特点**: 灵活的选择器配置
- **应用**: 数据采集、内容监控

### 4.3 批量数据处理
- **目标**: 自动化批量处理多个页面
- **特点**: 支持无人值守运行
- **应用**: 大规模数据采集、定期数据更新

## 5. 技术特性

### 5.1 可靠性
- **错误处理**: 完善的异常处理机制
- **重试机制**: 支持操作重试
- **会话恢复**: 自动会话状态恢复

### 5.2 可扩展性
- **模块化设计**: 清晰的模块分离
- **插件架构**: 支持自定义 OTP 提供器
- **配置驱动**: 基于配置的灵活部署

### 5.3 易用性
- **命令行工具**: 简单易用的命令行接口
- **配置管理**: 环境变量和配置文件支持
- **文档完善**: 详细的使用说明和示例

## 6. 部署要求

### 6.1 系统要求
- **操作系统**: Linux/Windows/macOS
- **Python 版本**: 3.8 或更高版本
- **内存要求**: 建议 2GB 以上
- **网络要求**: 稳定的网络连接

### 6.2 依赖包
- playwright==1.46.0
- python-dotenv==1.0.1
- tenacity==8.5.0
- pyotp==2.9.0

### 6.3 浏览器要求
- **Playwright 浏览器**: 自动下载和管理
- **Chromium**: 主要浏览器引擎

## 7. 安全考虑

### 7.1 凭据管理
- **环境变量**: 敏感信息通过环境变量配置
- **文件权限**: 会话文件权限控制
- **加密存储**: 建议对敏感配置进行加密

### 7.2 网络安全
- **HTTPS 支持**: 支持 HTTPS 连接
- **代理支持**: 支持企业代理配置
- **证书验证**: 可配置证书验证策略

## 8. 性能指标

### 8.1 响应时间
- **页面加载**: 默认 30 秒超时
- **元素等待**: 15 秒元素可见等待
- **网络空闲**: 智能网络空闲检测

### 8.2 资源使用
- **内存占用**: 基于 Playwright 的内存使用
- **CPU 使用**: 异步操作，低 CPU 占用
- **网络带宽**: 可配置慢动作模式控制

## 9. 维护和扩展

### 9.1 日志记录
- **操作日志**: 记录关键操作和错误
- **调试信息**: 支持详细调试输出
- **性能监控**: 记录操作耗时

### 9.2 版本管理
- **语义版本**: 遵循语义版本规范
- **向后兼容**: 保持 API 向后兼容
- **升级路径**: 提供平滑升级方案

## 10. 测试策略

### 10.1 单元测试
- **模块测试**: 各模块独立测试
- **Mock 支持**: 支持外部依赖 Mock
- **覆盖率**: 目标 80% 以上代码覆盖率

### 10.2 集成测试
- **端到端测试**: 完整流程测试
- **环境测试**: 多环境兼容性测试
- **性能测试**: 负载和压力测试

---

*本文档版本: 1.0*  
*最后更新: 2024年*  
*维护者: 项目开发团队*
