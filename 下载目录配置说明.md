# 下载目录配置说明

## 概述
系统支持配置自定义下载目录，可以通过环境变量或配置文件来指定文件下载和解压的位置。

## 配置方法

### 1. 环境变量配置
在 `.env` 文件中设置 `SP_DOWNLOAD_PATH` 环境变量：

```bash
# 相对路径（相对于当前运行目录）
SP_DOWNLOAD_PATH=downloads
SP_DOWNLOAD_PATH=./my_downloads
SP_DOWNLOAD_PATH=../shared_downloads

# 绝对路径
SP_DOWNLOAD_PATH=/home/user/downloads
SP_DOWNLOAD_PATH=/home/ki-zj-1586/下载
SP_DOWNLOAD_PATH=/data/sharepoint_downloads
```

### 2. 路径优先级
系统按以下优先级查找下载文件：

1. **配置的下载目录**（如果设置了 `SP_DOWNLOAD_PATH`）
   - 绝对路径：直接使用
   - 相对路径：相对于当前运行目录

2. **当前运行目录下的downloads文件夹**
   - `./downloads/文件名.zip`

3. **系统默认下载目录**
   - `~/下载/文件名.zip`（中文系统）
   - `~/Downloads/文件名.zip`（英文系统）

4. **项目根目录下的downloads**
   - `downloads/文件名.zip`

## 使用示例

### 示例1：使用相对路径
```bash
# 在 .env 文件中设置
SP_DOWNLOAD_PATH=downloads

# 文件将下载到：/home/ki-zj-1586/work/nj/WebScraping/downloads/
```

### 示例2：使用绝对路径
```bash
# 在 .env 文件中设置
SP_DOWNLOAD_PATH=/home/ki-zj-1586/下载

# 文件将下载到：/home/ki-zj-1586/下载/
```

### 示例3：使用自定义目录名
```bash
# 在 .env 文件中设置
SP_DOWNLOAD_PATH=./sharepoint_files

# 文件将下载到：/home/ki-zj-1586/work/nj/WebScraping/sharepoint_files/
```

## 解压目录结构
无论文件下载到哪个目录，解压后的文件都会按照以下结构组织：

```
downloads/
└── original_structure/
    └── Test/                    # 以Test为根目录
        └── 202509/
            └── 20250901_走行データ送付/
                └── 360/
                    └── 日本/
                        └── JP4テスト車/
                            └── 20250827/
                                ├── ziptest.zip
                                └── [解压后的文件]
```

## 注意事项

1. **目录权限**：确保配置的下载目录有读写权限
2. **磁盘空间**：确保有足够的磁盘空间存储下载和解压的文件
3. **路径格式**：使用正斜杠 `/` 作为路径分隔符
4. **相对路径**：相对路径是相对于脚本运行时的当前工作目录

## 验证配置
运行脚本后，查看日志输出中的文件查找信息：
```
📁 在 /path/to/your/downloads/filename.zip 找到zip文件
```

这表示系统成功在配置的目录中找到了下载的文件。
